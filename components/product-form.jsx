'use client';

import Link from 'next/link';
import { FigmaIcon } from './figma-icon';
import { mainCategories, legacyCategories } from '../lib/categories';
import { useRouter } from 'next/navigation';
import { useState } from 'react';

export function ProductForm({ product, options = [], initialVariants = [] }) {
  const router = useRouter();
  const [error, setError] = useState('');
  const [variantMode, setVariantMode] = useState(Boolean(product?.has_variants));
  const [variants, setVariants] = useState(initialVariants.map(row => ({ colorId: String(row.color_id), sizeId: String(row.size_id), price: String(row.price), stock: String(row.stock) })));
  const colors = options.filter(option => option.kind === 'color');
  const sizes = options.filter(option => option.kind === 'size');
  function changeVariant(index, field, value) { setVariants(current => current.map((row, i) => i === index ? { ...row, [field]: value } : row)); }
  const [saving, setSaving] = useState(false);
  const [slugTouched, setSlugTouched] = useState(Boolean(product));
  const [slug, setSlug] = useState(product?.slug || '');
  function slugify(value) { return value.toLowerCase().trim().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, ''); }
  async function save(event) {
    event.preventDefault(); setSaving(true); setError('');
    const form = new FormData(event.currentTarget);
    if (variantMode && !variants.length) { setError('Tambahkan setidaknya satu kombinasi warna dan ukuran.'); setSaving(false); return; }
    const body = { name: form.get('name'), slug, description: form.get('description'), category: form.get('category'), imageUrl: form.get('imageUrl'), price: form.get('price'), compareAtPrice: form.get('compareAtPrice'), stock: form.get('stock'), badge: form.get('badge'), featured: form.has('featured'), newest: form.has('newest'), active: form.has('active'), variants: variantMode ? variants : [] };
    try {
      const response = await fetch(product ? `/api/admin/products/${product.id}` : '/api/admin/products', { method: product ? 'PUT' : 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) });
      const result = await response.json();
      if (!response.ok) throw new Error(result.error || 'Gagal menyimpan produk.');
      router.push('/admin/products'); router.refresh();
    } catch (cause) { setError(cause.message); setSaving(false); }
  }
  return <form onSubmit={save} className="product-editor"><div className="editor-main"><section className="editor-card"><h2>Informasi produk</h2><div className="admin-field-grid"><label className="wide">Nama produk<input name="name" required maxLength="160" defaultValue={product?.name || ''} onChange={event => { if (!slugTouched) setSlug(slugify(event.target.value)); }} /></label><label className="wide">Slug URL<input name="slug" required maxLength="180" value={slug} onChange={event => { setSlugTouched(true); setSlug(slugify(event.target.value)); }} /><small>Huruf kecil, angka, dan tanda hubung.</small></label><label>Kategori<span className="select-field"><select name="category" required defaultValue={product?.category || ''}><option value="" disabled>Pilih kategori</option><optgroup label="Kategori toko">{mainCategories.map(category => <option key={category} value={category}>{category}</option>)}</optgroup><optgroup label="Kategori produk lama">{legacyCategories.map(category => <option key={category} value={category}>{category}</option>)}</optgroup></select><FigmaIcon name="arrow-right" /></span></label><label>Label (opsional)<input name="badge" maxLength="40" defaultValue={product?.badge || ''} placeholder="Contoh: TERLARIS" /></label><label className="wide">Deskripsi<textarea name="description" rows="5" maxLength="2000" defaultValue={product?.description || ''} /></label></div></section><section className="editor-card"><h2>Harga & stok</h2><label className="admin-check"><input type="checkbox" checked={variantMode} onChange={event => setVariantMode(event.target.checked)} /> Produk memiliki pilihan warna dan ukuran</label>{!variantMode && <div className="admin-field-grid"><label>Harga jual (Rp)<input name="price" type="number" min="0" step="1" required defaultValue={product?.price ?? ''} /></label><label>Harga coret (Rp)<input name="compareAtPrice" type="number" min="0" step="1" defaultValue={product?.compare_at_price ?? ''} /></label><label>Stok<input name="stock" type="number" min="0" step="1" required defaultValue={product?.stock ?? 0} /></label></div>}{variantMode && <div className="variant-editor"><p>Harga katalog mengikuti kombinasi termurah; stok katalog adalah jumlah seluruh kombinasi.</p>{(!colors.length || !sizes.length) && <p>Tambahkan warna dan ukuran di <Link href="/admin/variants">Master varian</Link> terlebih dahulu.</p>}{variants.map((row, index) => <div className="variant-row" key={index}><label>Warna<select value={row.colorId} onChange={event => changeVariant(index, "colorId", event.target.value)} required><option value="">Pilih warna</option>{colors.map(option => <option key={option.id} value={option.id}>{option.name}</option>)}</select></label><label>Ukuran<select value={row.sizeId} onChange={event => changeVariant(index, "sizeId", event.target.value)} required><option value="">Pilih ukuran</option>{sizes.map(option => <option key={option.id} value={option.id}>{option.name}</option>)}</select></label><label>Harga jual (Rp)<input type="number" min="0" step="1" value={row.price} onChange={event => changeVariant(index, "price", event.target.value)} required /></label><label>Stok<input type="number" min="0" step="1" value={row.stock} onChange={event => changeVariant(index, "stock", event.target.value)} required /></label><button type="button" className="remove-link" onClick={() => setVariants(current => current.filter((_, i) => i !== index))}>Hapus</button></div>)}<button type="button" className="admin-secondary" onClick={() => setVariants(current => [...current, { colorId: "", sizeId: "", price: "", stock: "0" }])}>+ Tambah kombinasi</button></div>}</section></div><aside className="editor-side"><section className="editor-card"><h2>Gambar produk</h2><label>URL gambar<input name="imageUrl" type="url" required maxLength="1000" defaultValue={product?.image_url || ''} placeholder="https://..." /></label>{product?.image_url && <img className="editor-preview" src={product.image_url} alt={product.name} />}</section><section className="editor-card"><h2>Publikasi</h2><label className="admin-check"><input name="active" type="checkbox" defaultChecked={product?.is_active ?? true} /> Tampilkan di toko</label><label className="admin-check"><input name="featured" type="checkbox" defaultChecked={product?.is_featured ?? false} /> Best Seller</label><label className="admin-check"><input name="newest" type="checkbox" defaultChecked={product?.is_new ?? false} /> New Arrival</label></section>{error && <p className="admin-error" role="alert">{error}</p>}<div className="editor-buttons"><button className="admin-primary" disabled={saving}>{saving ? 'Menyimpan...' : 'Simpan produk →'}</button><Link href="/admin/products">Batal</Link></div></aside></form>;
}
