# Memori Proyek Aira Daily Store

**Terakhir diperbarui:** 24 September 2026
**Status:** aplikasi dan panel admin selesai sebagai prototipe; skema dan data contoh ada di Supabase, beranda serta katalog sudah aktif di Vercel, tetapi checkout dan admin produksi belum diuji penuh.
**Repo:** `/Users/yukiwicaksono/Devs/aira_daily_store`

## Ringkasan pekerjaan

- Toko Next.js App Router dengan beranda, katalog (pencarian, kategori, urutan harga), detail produk, keranjang browser, dan checkout.
- Checkout menyimpan `orders` dan `order_items` di PostgreSQL serta mengurangi stok dalam satu transaksi. Harga dihitung ulang di server. Setelah commit berhasil, checkout mencoba mengirim konfirmasi WhatsApp melalui Meta Cloud API dan email melalui SMTP Gmail ke kontak pelanggan; kegagalan notifikasi tidak membatalkan pesanan.
- Panel `/admin/login` dengan akun tunggal dari environment, password scrypt, cookie sesi bertanda tangan, dan pemeriksaan asal permintaan untuk mutasi.
- Panel `/admin` menyediakan ringkasan, CRUD produk (arsip alih-alih hapus permanen), stok, serta status pesanan. Pembatalan pesanan `pending` mengembalikan stok satu kali.
- Koneksi Supabase tetap melalui `pg` di server Next.js. `DATABASE_URL` untuk runtime; `MIGRATION_DATABASE_URL` untuk migrasi dan seed. Pool dibatasi satu koneksi per instance. URL Supabase memakai `sslmode=verify-full` dan sertifikat `prod-ca-2021.crt` dari dashboard secara otomatis, sehingga TLS memverifikasi CA serta hostname tanpa warning `pg-connection-string`.
- `.env` dan `.env.example` memiliki `NEXT_PUBLIC_SUPABASE_URL` serta `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. Bila keduanya terisi, beranda/katalog/detail produk membaca lewat Supabase JS/Data API; bila kosong, katalog memakai `pg` lokal. Checkout dan admin tetap memakai `pg` untuk transaksi dan akses administratif. Konfigurasi yang hanya terisi satu variabel menghasilkan error yang jelas.
- `db:migrate` hanya membuat/memperbarui skema; `db:seed` secara opsional memasukkan 12 produk dan 3 pesanan demo dengan 4 item pesanan. Seed aman diulang, menjaga stok pesanan aktif, dan tidak menimpa data yang sudah ada. RLS produk mengizinkan role anon/authenticated membaca produk aktif saja. Tabel `orders` dan `order_items` tetap tertutup dari Data API publik.
- Panduan setup ada di [README.md](README.md). Aset dari `/Users/yukiwicaksono/Devs/kaija-nextjs-asset` berupa starter kode; gambar produk saat ini memakai URL Unsplash remote.

## Bukti terakhir

- `npm run build` berhasil setelah persiapan Supabase.
- Setelah pengaturan SSL, parser `pg-connection-string` membaca kedua URL dari `.env` dengan `sslmode=verify-full` tanpa warning; `npm run test:catalog` (3 tes) dan `npm run build` berhasil. Ini belum membuktikan koneksi ke Supabase.
- `db:ping` menguji koneksi dengan `SELECT 1` tanpa mengubah data dan hanya menampilkan kode kegagalan, bukan URL atau password.
- Pemeriksaan jaringan pada host kerja (23 September 2026): IPv4 publik berfungsi; antarmuka hanya memiliki IPv6 loopback/link-local, tanpa alamat IPv6 global. Akses Direct Supabase IPv6 tidak dapat dirutekan dari jaringan saat ini. Kondisi ini dapat berubah jika jaringan/VPN berubah.
- `npm run test:catalog` memverifikasi URL/key dari environment dipakai oleh SDK untuk permintaan katalog dan detail. Pada PostgreSQL uji dengan role `anon`/`authenticated`, migrasi memberi `anon` SELECT produk saja (tanpa INSERT atau SELECT pesanan); role tersebut melihat 11 produk aktif dari 12 total setelah satu diarsipkan. Migrasi ulang tidak menggandakan policy. Jalur katalog `pg` lokal tetap membaca 11 produk aktif.
- Setelah nilai `NEXT_PUBLIC_` diisi atau diubah, build produksi harus dijalankan ulang agar bundle Next.js memakai nilai terbaru.
- Pada PostgreSQL uji lokal yang terpisah: `db:migrate` membuat tabel dan RLS dengan **0 produk**; `db:check` berhasil; `db:seed` menghasilkan **12 produk**; migrasi ulang mempertahankan 12 produk.
- Uji sebelumnya: halaman toko, katalog, detail, dan admin berhasil dimuat. API admin menolak akses tanpa sesi; tambah/edit/arsip produk berhasil; produk arsip hilang dari toko. Checkout menolak stok berlebih. Pembatalan pesanan mengembalikan stok dan pembatalan ulang ditolak; status `paid` tersimpan.
- Database uji lokal berada di `/private/tmp/aira-pg-test` (port 55432) dan **servernya sudah dihentikan**. Ini bukan database aplikasi permanen.
- `.env` kini memiliki Project URL, publishable key, `DATABASE_URL`, dan `MIGRATION_DATABASE_URL` yang terisi. Kedua URL database mengarah ke Session pooler port 5432 pada proyek Supabase yang sama. Perintah `db:*` membaca `.env` secara langsung. Jangan mencatat kredensial di file ini.
- Pada 23 September 2026, `npm run db:ping -- --migration` berhasil setelah CA Supabase dimuat. `npm run db:migrate` berhasil pada proyek Supabase pengguna. `npm run db:check` menampilkan `products=true`, `orders=true`, `order_items=true` serta koneksi ke database `postgres`; proses pemeriksaan tersebut tidak segera keluar dan dihentikan manual setelah hasil tampil. `npm run db:ping` untuk runtime juga menampilkan koneksi berhasil tetapi tidak segera keluar. Perlu menelusuri penutupan koneksi Session pooler jika berulang.
- `npm run build` berhasil setelah konfigurasi CA; parser `pg` menunjukkan CA termuat serta verifikasi TLS aktif untuk kedua URL.
- Pada proyek Supabase pengguna, `npm run db:seed` menambahkan 12 produk, 3 pesanan demo (`pending`, `paid`, `cancelled`), dan 4 item pesanan. Pemeriksaan sesudahnya: 12/3/4 baris, 0 item tanpa relasi, 0 pesanan dengan total salah. Menjalankan `db:seed` ulang menambahkan 0 baris.
- Pada 24 September 2026, halaman `/admin` di Next.js dev sempat gagal karena `fileURLToPath(new URL(...))` pada `lib/db-config.js` menerima objek URL dari bundler yang tidak cocok. Path sertifikat sekarang ditentukan dari root proses. Halaman admin kembali memuat ringkasan toko; `npm run build` lulus dan parser `pg` tetap memuat CA dengan verifikasi TLS aktif.
- Pada 24 September 2026, investigasi jeda **Rendering** di dev: Next.js mengompilasi route saat dibuka dan seluruh halaman katalog/admin dirender dinamis. Pengukuran baca data sebelum perubahan: dua query katalog beranda sekitar 1,5 detik pada koneksi baru; tiga query ringkasan admin dengan pool satu koneksi sekitar 3,5 detik. Ringkasan admin sekarang satu query (pengukuran koneksi baru sekitar 2,5 detik), beranda mengambil dua kelompok produk lewat satu permintaan, dan loading boundary memberi umpan balik saat navigasi. `npm run build`, 3 tes katalog, serta halaman beranda dan ringkasan admin di browser lulus. Waktu tersisa terutama bergantung pada koneksi Supabase dan mode dev; belum ada pengukuran produksi.
- Tampilan loading toko dan admin kini memakai spinner CSS di tengah area konten, teks status, dan penghormatan pada preferensi gerakan berkurang. Build berhasil; tampilan loading keduanya diperiksa secara visual saat navigasi di browser.
- Area loading toko sekarang memenuhi tinggi viewport yang tersisa di bawah ticker/header (desktop dan mobile), sehingga footer baru terlihat setelah pengguna scroll. Screenshot saat navigasi ke katalog membuktikan spinner tetap di tengah dan footer tidak tampak di layar awal; build berhasil.
- Pada 24 September 2026, ikon yang relevan dari file Figma Community “36 Ecommerce icons” diunduh sebagai SVG lokal ke `public/icons/`. Komponen `FigmaIcon` memakai ukuran asli aset dalam bingkai 24 px. Ikon diterapkan pada navigasi toko dan ponsel, pencarian katalog, keranjang serta tombol tambah keranjang, menu admin, dan aksi edit produk. Build berhasil; halaman produk dan daftar produk admin diperiksa secara visual di browser. Aset SVG tambahan (bintang, truk, panah) tersedia untuk kebutuhan tampilan berikutnya.
- Pada 24 September 2026, fallback env bawaan integrasi Vercel yang sempat ditambahkan dibatalkan atas permintaan pengguna. Aplikasi kembali membaca `DATABASE_URL` untuk runtime, `MIGRATION_DATABASE_URL` untuk migrasi (fallback ke `DATABASE_URL`), dan pasangan `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` untuk katalog. `npm run test:catalog` (3 tes) dan `npm run build` lulus setelah rollback. Variabel tersebut tetap perlu disetel pada environment deployment Vercel yang sesuai.
- Pada 24 September 2026 pukul 10:20 WIB, deployment Production Vercel `8vUyBLjsWX8GR35kQudyQpjQaYbX` diredeploy dari commit `3c3865e` dengan Project Settings terbaru. Status Ready dan domain `airadaily.store` terpasang. Beranda tampil dengan produk, `/shop` menampilkan 12 produk, dan log runtime khusus deployment baru menunjukkan request 200 tanpa error pada pemeriksaan tersebut. Sebelumnya digest `2572647074` cocok dengan log `DATABASE_URL belum diatur` pada deployment lama. Login admin dan checkout produksi belum diuji.
- Pada 24 September 2026, simbol dekoratif dan tanda sukses berbasis karakter diganti dengan SVG Figma lokal yang sudah dipakai pada navigasi; teks “Outdoor Kids” di header serta metadata dihapus. Dropdown kategori/urutan katalog dan kategori produk admin dibuat kotak datar dengan indikator SVG, ukuran teks 16 px, serta susunan satu kolom di ponsel. Nama dan slug produk contoh baru memakai “Silas Kids Vest”; `db:seed` akan menyesuaikan produk dan item pesanan demo lama tanpa menggandakan produk. `npm run test:catalog` (3 tes), `npm run build`, dan `git diff --check` lulus. Pemeriksaan visual lokal belum bisa dilakukan karena server lokal ditolak (`listen EPERM`), dan pembaruan data Supabase belum bisa dijalankan karena DNS koneksi database gagal (`ENOTFOUND`).
- Pada 24 September 2026, arah UI/UX toko diubah menjadi reseller kebutuhan rumah dan keluarga: beranda, katalog, header, ticker, footer, keranjang, detail, metadata, serta login admin memakai diksi sprei/pakaian harian tanpa tema outdoor. Foto hero lokal `public/hero-home.jpg` (340 KB) dibuat untuk menampilkan sprei dan pakaian keluarga; kartu kategori beranda ditambah. Kategori baru `Sprei & Kamar`, `Baju Anak`, `Baju Dewasa`, `Tas & Aksesori`, dan `Lainnya` tersedia di filter, form admin, serta validasi API. Kategori lama tetap tersedia agar produk yang ada dapat diakses. Seed produk baru menggunakan kategori dan deskripsi umum; data contoh yang sudah tersimpan di Supabase belum diselaraskan atau diganti dengan inventaris asli. Build, 3 tes katalog, dan validasi 5 kategori baru lulus. Pemeriksaan visual lokal kembali terhalang `listen EPERM` pada `127.0.0.1:3100`.
- Pada 24 September 2026, seluruh simbol `↗` di UI diganti dengan aset Figma `arrow-top-right-normal` dari file Arrows Icons, diekspor sebagai `public/icons/arrow-up-right.svg` (24×24), lalu didaftarkan di `FigmaIcon`. Tombol, tautan section, empty state, checkout, dan panel admin memakai ikon yang sama dengan aturan warna kontras. `rg` tidak menemukan lagi `↗`; `npm run build`, `npm run test:catalog`, dan `git diff --check` lulus.
- Pada 24 September 2026, logo teks Aira Daily di header toko, footer, dan panel admin diganti dengan aset pengguna `public/aira-daily-logo.png` (500×500 RGBA, 152 KB). Ukuran responsif ditambahkan untuk desktop dan mobile, termasuk logo admin. `npm run build` dan `git diff --check` lulus.
- Pada 24 September 2026, logo di header, footer, dan panel admin diberi efek `drop-shadow` yang mengikuti bentuk transparan gambar. Intensitas bayangan kemudian sedikit dinaikkan menjadi offset 7 px, blur 11 px, opasitas 0,34 atas permintaan pengguna. `npm run build` dan `git diff --check` lulus.
- Pada 24 September 2026, ticker paling atas dan deretan pesan beranda mulai dari “PILIHAN UNTUK RUMAH” diberi animasi berjalan pelan yang berulang mulus. Salinan animasi disembunyikan dari pembaca layar, dan preferensi gerakan berkurang menonaktifkan animasi serta mengizinkan gulir manual. `npm run build` dan `git diff --check` lulus.
- Pada 24 September 2026, tombol tambah keranjang di kartu dan halaman detail kini berubah hijau, memunculkan centang dengan animasi singkat, dan menampilkan pesan sukses setelah item ditambahkan. Gambar produk juga mengecil dan terbang menuju ikon keranjang di header desktop atau menu bawah ponsel, lalu ikon tujuan memantul singkat. Jika jumlah produk di keranjang sudah mencapai stok, tombol memberi pesan batas stok tanpa mengklaim penambahan berhasil dan tanpa animasi terbang. Aksi menunggu data keranjang selesai dimuat; preferensi gerakan berkurang menonaktifkan animasi. `npm run build` dan `git diff --check` lulus. Pemeriksaan visual lokal terhalang `listen EPERM` pada port 3100. Perubahan masuk commit `78efacb`.
- Pada 24 September 2026, header toko diberi `position: sticky` dengan `top: 0` di desktop, sehingga tetap terlihat hingga bagian bawah halaman saat scroll; ticker di atasnya tetap mengikuti aliran halaman. Pada layar maksimal 640 px, header tetap tampil seperti semula dan ikut bergulir, sementara menu navigasi bawah tetap terpasang. `npm run build` dan `git diff --check` lulus.
- Pada 24 September 2026, pencarian header diubah dari tautan langsung ke `/shop` menjadi input autocomplete. Setelah minimal 2 karakter, header mengambil maksimal 6 produk relevan dari route publik `/api/products/search` dan menampilkan gambar, nama, kategori, serta harga; hasil dapat dibuka langsung ke detail produk dan tersedia tautan ke seluruh hasil di `/shop?q=...`. `npm run build`, `npm run test:catalog`, dan `git diff --check` lulus.
- Pada 24 September 2026, checkout ditambah konfirmasi otomatis ke nomor WhatsApp Indonesia dan email yang diisi pelanggan. Nomor dinormalisasi ke format `62...`. WhatsApp memakai template Meta `aira_order_received` bahasa `id` (nama, nomor pesanan, subtotal); pengirim harus terdaftar sebagai `6285111412046` lewat Phone Number ID Meta. Email dikirim dari `it.airasolutions@gmail.com` memakai SMTP Gmail dan App Password. Pengiriman dilakukan setelah transaksi database commit, kedua kanal dicoba secara independen, dan halaman sukses menampilkan status masing-masing. `npm run test:notifications` (3 tes), `npm run test:catalog` (3 tes), `npm run build`, dan `git diff --check` lulus. Tes memakai mock untuk API Meta dan SMTP; pengiriman nyata belum diuji karena token Meta, template yang disetujui, dan Google App Password belum dikonfigurasi di environment.
- Pada 24 September 2026, halaman `/admin/settings` ditambah untuk mengubah nomor WhatsApp pengirim, Meta Phone Number ID/versi API/access token, serta alamat Gmail pengirim/App Password. Kredensial baru dienkripsi AES-256-GCM di tabel privat `notification_settings` dengan kunci turunan `ADMIN_SESSION_SECRET`; admin hanya melihat indikator kredensial tersedia. Checkout membaca pengaturan database pada tiap pesanan, dengan environment lama sebagai fallback. `npm run db:migrate` melaporkan sukses membuat skema pada database tujuan dan mempertahankan data lama. `npm run test:notifications` (4 tes), `npm run test:catalog` (3 tes), `npm run build`, dan `git diff --check` lulus. Pemeriksaan SELECT terpisah melalui URL runtime maupun migrasi gagal `ENOTFOUND` untuk host pooler setelah migrasi, sehingga keberadaan baris belum terkonfirmasi secara independen. Pengaturan belum diuji melalui login admin atau kirim nyata.
- Pada 24 September 2026, admin telah mengisi Google App Password di `/admin/settings`. Tombol **Kirim email uji ke alamat pengirim** ditambahkan dengan API yang memerlukan sesi admin dan pemeriksaan origin. Satu email uji dikirim melalui aplikasi lokal ke `it.airasolutions@gmail.com`; server Gmail menerima permintaan dan pesan berjudul “Uji email Aira Daily Store” terlihat di Kotak Masuk Gmail pukul 15.38 WIB. Ini membuktikan SMTP dan kredensial pada aplikasi lokal berfungsi, belum membuktikan checkout produksi. `npm run test:notifications` (4 tes) dan `npm run build` lulus. Pembacaan database langsung dari terminal masih gagal DNS pooler (`ENOTFOUND`), meskipun halaman admin lokal dapat membaca pengaturan.
- Pada 24 September 2026, persyaratan persetujuan penerima WhatsApp dari kebijakan Meta diterapkan di checkout: pelanggan dapat mencentang persetujuan eksplisit atas nama Aira Daily, nilai `whatsapp_opt_in` disimpan di pesanan, dan server hanya mengirim WhatsApp jika bernilai benar. Pesanan tanpa persetujuan tetap memperoleh email. `npm run db:migrate` berhasil menambahkan kolom secara kompatibel; `npm run test:notifications` (5 tes), `npm run test:catalog` (3 tes), `npm run build`, dan `git diff --check` lulus. Sumber kebijakan: https://business.whatsapp.com/policy/preview?lang=id_ID.
- Pada 24 September 2026, email konfirmasi pesanan diperbarui dengan subjek bernomor invoice `INV-YYYYMMDD-ID`, isi HTML responsif dan teks biasa, rincian barang/alamat, serta lampiran invoice PDF A4 yang dibuat dari data pesanan setelah commit. PDF menandai ongkir dan pembayaran masih menunggu konfirmasi. Tombol admin kini mengirim pratinjau email beserta PDF bertanda contoh. Uji nyata dari aplikasi lokal: Gmail menerima email pratinjau pukul 16.15 WIB; isi HTML dan lampiran `INV-20260924-000000-CONTOH.pdf` terlihat di kotak masuk. PDF contoh satu halaman dan uji 50 item lima halaman dibuat; `file` mengenalinya sebagai PDF. `npm run test:notifications` (6 tes) dan `npm run build` lulus. Visual PDF belum bisa dirender di host ini karena Poppler/Python renderer tidak tersedia dan instalasi paket Python terhalang DNS; pratinjau PDF masih perlu diperiksa secara visual oleh pengguna.
- Pada 24 September 2026, nomor pesanan tampilan menjadi `ORD-YYYYMMDD-000000`, mengikuti tanggal Asia/Jakarta dan ID database yang dipad minimal enam digit. ID asli, relasi, dan endpoint mutasi admin tetap sama; nomor tampilan muncul pada sukses checkout, daftar/ringkasan admin, email, WhatsApp, dan PDF invoice. Nomor invoice tetap `INV-YYYYMMDD-000000` dengan aturan tanggal/ID yang sama. Nomor diturunkan saat dibaca, termasuk untuk pesanan lama, sehingga tidak memerlukan migrasi atau kolom unik tambahan. PDF contoh diperbarui. `npm run test:notifications` (7 tes, termasuk batas tanggal Jakarta dan ID lebih dari enam digit), `npm run test:catalog` (3 tes), dan `npm run build` lulus. Pengiriman nyata dengan nomor baru belum diuji.

## Yang masih perlu dikerjakan

1. [x] Isi `MIGRATION_DATABASE_URL` dan `DATABASE_URL` dengan Session pooler; Project URL dan publishable key sudah terisi. Jangan taruh password database pada variabel `NEXT_PUBLIC_`.
2. [x] Jalankan `npm run db:migrate`, periksa tabel, lalu isi semua tabel dengan data contoh atas permintaan pengguna.
3. [ ] Telusuri proses `db:check` dan `db:ping` runtime yang tidak segera keluar setelah query berhasil pada Session pooler.
4. [ ] Siapkan `ADMIN_EMAIL`, `ADMIN_PASSWORD_HASH` (`npm run admin:hash`), dan `ADMIN_SESSION_SECRET`; coba login admin pada lingkungan tujuan.
5. [ ] Isi produk dan gambar asli melalui panel admin. Saat ini gambar contoh bergantung pada URL remote.
6. [ ] Uji alur beli dan pembatalan stok pada lingkungan Supabase, lalu tentukan kebutuhan pembayaran, ongkir, dan notifikasi sebelum peluncuran.
7. [ ] Pilih platform deployment dan pasang environment rahasia di sana; periksa koneksi pooler dan batas koneksi sesuai platform.
8. [ ] Saat koneksi Supabase tersedia, jalankan `npm run db:seed` untuk menyesuaikan nama/slug produk contoh lama, lalu periksa dropdown katalog dan form produk admin di ponsel.
9. [ ] Ganti data contoh lama di Supabase dengan inventaris reseller asli (termasuk sprei dan pakaian dewasa), lalu periksa beranda, filter kategori, dan hero pada desktop/ponsel di lingkungan yang bisa menjalankan server.
10. [ ] Daftarkan nomor `6285111412046` pada Meta WhatsApp Business Platform, setujui template `aira_order_received` bahasa `id`, lalu isi Phone Number ID, access token, dan versi API di `/admin/settings`. Meta dapat mengenakan biaya untuk template yang memulai percakapan.
11. [x] Buat Google App Password untuk `it.airasolutions@gmail.com`, isi di `/admin/settings`, lalu uji kirim dan penerimaan email melalui aplikasi lokal. Jangan catat token atau App Password di memori ini.
12. [ ] Setelah aplikasi versi baru dideploy, buat satu pesanan uji dengan nomor dan email yang dikuasai; pastikan notifikasi checkout benar-benar diterima. Saat DNS pooler pulih, periksa baris `notification_settings` secara independen.
13. [ ] Periksa PDF invoice contoh secara visual di perangkat pengguna, lalu uji satu checkout nyata untuk memastikan subjek, isi, dan lampiran invoice memakai data pesanan tersimpan.
14. [ ] Setelah deployment, periksa satu pesanan baru dan satu pesanan lama di admin untuk memastikan nomor `ORD-...` konsisten dengan email, WhatsApp, dan PDF; format template WhatsApp yang sudah disetujui di Meta mungkin masih menambah tanda `#` di depan nomor.

## Cara memantau progres

Perbarui tanggal, bukti, dan checklist di file ini setelah setiap tahap selesai. Untuk pemeriksaan cepat gunakan `npm run build` dan `npm run db:check`. Pada 24 September 2026, `npm run test:catalog` (3 tes) dan `npm run build` berhasil sebelum commit aplikasi.
